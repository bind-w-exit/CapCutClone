<Page
    x:Class="CapCutClone.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    CornerRadius="10"
    Style="{StaticResource PageStyle}"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource SystemControlPageBackgroundChromeLowBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Button
            Height="50"
            Margin="20"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Top"
            Content="Sign in"
            CornerRadius="5"
            FontWeight="Bold" />

        <Button
            Height="50"
            Margin="20"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Bottom"
            Click="Button_Click"
            Content="Settings"
            CornerRadius="5"
            FontWeight="Bold" />


        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="150" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Button
                Margin="20"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                CornerRadius="10"
                FontWeight="Bold">
                <Button.Content>
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <FontIcon
                            FontFamily="{StaticResource MaterialIcons}"
                            FontSize="25"
                            Foreground="LightBlue"
                            Glyph="&#xe147;" />
                        <TextBlock
                            VerticalAlignment="Center"
                            FontWeight="Bold"
                            Text="New project" />
                    </StackPanel>
                </Button.Content>
            </Button>

            <RelativePanel Grid.Row="1" Margin="20,0,20,0">
                <TextBlock
                    FontWeight="Bold"
                    RelativePanel.AlignVerticalCenterWithPanel="True"
                    Text="Projects" />
                <SearchBox
                    x:Name="SearchField"
                    Width="300"
                    Margin="0,0,10,0"
                    RelativePanel.LeftOf="ViewComboBox"
                    SearchHistoryEnabled="False" />
                <ComboBox x:Name="ViewComboBox" RelativePanel.AlignRightWithPanel="True" />
            </RelativePanel>

            <StackPanel
                Grid.Row="2"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Spacing="10">
                <FontIcon
                    FontFamily="{StaticResource MaterialIcons}"
                    FontSize="75"
                    Foreground="Gray"
                    Glyph="&#xe071;" />
                <TextBlock
                    VerticalAlignment="Center"
                    Foreground="Gray"
                    Text="Project edited will be saved automatically. Start creating your own videos!" />
            </StackPanel>

        </Grid>

    </Grid>
</Page>
